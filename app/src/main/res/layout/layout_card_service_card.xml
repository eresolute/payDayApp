<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:id="@+id/payday_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@mipmap/payday_card_dashboard"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/AppTheme.Text.Small"
        android:text="@string/change_pin"
        android:background="@drawable/bg_white"
        android:textColor="@color/textColor"
        android:padding="@dimen/dimen_4"
        android:layout_marginTop="@dimen/dimen_4"
        android:textAlignment="center"
        android:textSize="@dimen/text_12"
        app:layout_constraintStart_toStartOf="@id/payday_card"
        app:layout_constraintEnd_toEndOf="@id/payday_card"
        app:layout_constraintTop_toBottomOf="@id/payday_card"
        />

</android.support.constraint.ConstraintLayout>